apiVersion: v1
kind: Service
metadata:
  annotations:
    service.alpha.kubernetes.io/tolerate-unready-endpoints: "true"
  name: etax-identity-service
  labels:
    app: etax
spec:
  --type: NodePort
  ports:
  - port: 44540
    name: etax-identity-service-port
    targetPort: 44540
    --nodePort: 32756
  selector:
    component: etax-identity
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: etax-identity-deployment
spec:
  replicas: 3
  template:
    metadata:
      labels:
        app: etax
        component: etax-identity
        tier: backend
    spec:
      containers:
      - name: etax-identity-container
        image: dhanuwat/etax-identity:v1.0.0
        ports:
        - containerPort: 44540
      imagePullSecrets:
      - name: regcred